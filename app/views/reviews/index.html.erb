<div class="bg-teal p-12 flex justify-between items-center text-white">
  <h1 class="text-4xl">Reviews</h1>
  <%= link_to 'New Review', new_review_path, class: "rounded border-2 border-white hover:bg-white px-6 py-3 text-center text-white hover:text-teal" %>
</div>

<div class="p-12 grid lg:grid-cols-2 gap-6">
  <%= render @reviews %>
</div>

<% if @review %>
<%= render 'form' %>
<% end %>